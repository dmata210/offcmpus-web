mutation StatsStudentAccountCreation {
  Stats_StudentAccountCreation {
    v
  }
}

mutation StatsStudentLogin {
  Stats_StudentLogin {
    v
  }
}

mutation StatsLandlordAccountCreation {
  Stats_LandlordAccountCreation {
    v
  }
}

mutation StatsLandlordLogin($landlord_id: String!) {
  Stats_LandlordLogin(landlord_id: $landlord_id) {
    v
  }
}

mutation StatsLandlordOpenLease(
  $landlord_id: String!
  $property_id: String!
  $lease_id: String!
) {
  Stats_LandlordOpenLease(
    landlord_id: $landlord_id
    property_id: $property_id
    lease_id: $lease_id
  ) {
    v
  }
}
